name: Brand Kill List Lint

on: [push, pull_request]

jobs:
  brand-lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run Walking the Wild Kill List linter
        run: bash scripts/brand-lint.sh
